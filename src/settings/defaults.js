// Настройки плагина по умолчанию
// Этот объект сливается с пользовательскими настройками при загрузке
// Структура: ключ → значение по умолчанию

module.exports = {
  // API-ключ от Hugging Face (начинается с hf_)
  // Студент: никогда не коммитьте реальный ключ в репозиторий!
  apiKey: '',
  
  // Идентификатор модели на Hugging Face
  // Примеры: 'mistralai/Mistral-7B-Instruct-v0.3', 'meta-llama/Llama-2-7b-chat-hf'
  model: 'mistralai/Mistral-7B-Instruct-v0.3',
  
  // Максимальное количество токенов в ответе модели
  // 1 токен ≈ 0.75 слова. 1500 токенов ≈ 1100 слов
  maxTokens: 10000,
  
  // Лимит символов контекста из редактора, который отправляем в модель
  // Защита от переполнения и экономия токенов
  contextLimit: 4000,
  
  // Температура генерации: 0.0 — детерминированный ответ, 1.0 — креативный
  temperature: 0.6,

  // Максимальное количество пар вопросов-ответов, которое сохраняется в чате
  maxHistoryLength: 20, 
  
  // Пользовательские быстрые режимы (можно добавлять без изменения кода)
  // Формат: { id: 'уникальный_ключ', label: 'Текст кнопки', instruction: 'Промпт для модели' }
  customPrompts: [],

  // История чата (для восстановления контекста при перезапуске Obsidian)
  chatHistory: []
  
}; 
